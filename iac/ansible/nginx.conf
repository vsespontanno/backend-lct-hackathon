events {}

http {
    server {
        listen 80;
        location / {
            return 200 'Hello from Terraform + Ansible!';
            add_header Content-Type text/plain;

        }
        location /health {
            return 200 'OK';
            add_header Content-Type text/plain;
        }
    }
}